extern crate loom;
/*
use std::path::Path;
use std::env;
use loom::render::render_file;

fn main() {
    for arg in env::args().skip(1) {
        let path = Path::new(&arg);
        render_file(path, 700.)
        .save(path.with_extension("png")).expect("failed to write image");
    }
}
*/
fn main() {}

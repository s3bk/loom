pub type Length = f32; 
pub type Scale = f32;
pub type Point = (f32, f32);
pub type Size = (f32, f32);

/*
pub struct Rect {
    x:      f32,
    y:      f32,
    width:  f32,
    height: f32,
    
}

pub struct Trafo {
    x:  (Length, Scale),
    y:  (Length, Scale)
}
*/
